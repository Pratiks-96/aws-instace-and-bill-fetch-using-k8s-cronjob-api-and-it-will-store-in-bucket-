apiVersion: batch/v1
kind: CronJob
metadata:
  name: aws-pdf-report
spec:
  schedule: "*/2 * * * *"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: aws-report
            image: pratik6958/aws_instace:latest

            env:
            - name: AWS_ACCESS_KEY_ID
              value: "your access key "

            - name: AWS_SECRET_ACCESS_KEY
              value: "sercrete access key"

            - name: AWS_DEFAULT_REGION
              value: "us-east-1"

            - name: S3_BUCKET
              value: "ec2--files"

          restartPolicy: Never
